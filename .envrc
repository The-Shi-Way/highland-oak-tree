# direnv configuration — auto-loads env vars when entering the project
# Install direnv: https://direnv.net/docs/installation.html
# Run `direnv allow` after creating .env.local

dotenv_if_exists server-nestjs/.env.local
dotenv_if_exists client/.env.local

export NODE_ENV=development
